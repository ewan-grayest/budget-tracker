# The image needs the application and its entrypoint, nothing else. Excluding
# everything by default keeps the build context tiny and guarantees a local
# data/ directory with a real budget.db can never reach the daemon.
*
!app.py
!docker-entrypoint.py
